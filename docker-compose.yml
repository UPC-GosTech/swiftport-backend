services:
  swiftport-api:
    image: swiftport-api:0.0.3
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8080:8080
    environment:
      - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE}
      - LCPASSWORD=${LCPASSWORD}